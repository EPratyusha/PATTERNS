#include <stdio.h>

int main()
{
    int rows=10,i,j;
    for(i=1;i<=rows;i++)
    {
        for(j=1;j<=rows;j++)
        {
         if(j==1 || i==1 || i==(rows/2) || i==rows)
         printf("*");
         else
         printf(" ");
        }
        printf("\n");
    }

    return 0;
}

